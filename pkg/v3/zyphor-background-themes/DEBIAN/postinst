#!/bin/sh
set -e

THEME="/usr/share/desktop-base/zyphor-os-theme"

case "$1" in
    configure)
        if [ -d "$THEME" ]; then
            update-alternatives \
                --install \
                /etc/alternatives/desktop-themes \
                desktop-themes \
                "$THEME" \
                100

            update-alternatives \
                --set \
                desktop-themes \
                "$THEME"
        fi
        ;;
esac

exit 0